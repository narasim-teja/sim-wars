[package]
name = "program-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
litesvm = "0.11"
litesvm-token = "0.11"
solana-sdk = "3.0"
solana-program = "3.0"
spl-token = "9.0"
spl-associated-token-account = "8.0"
anchor-lang = "1.0.0"
token-mint = { path = "../programs/token-mint", features = ["no-entrypoint"] }
amm-dex = { path = "../programs/amm-dex", features = ["no-entrypoint"] }
staking = { path = "../programs/staking", features = ["no-entrypoint"] }
governance = { path = "../programs/governance", features = ["no-entrypoint"] }
